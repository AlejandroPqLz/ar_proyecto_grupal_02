[project]
name = "ar_proyecto_grupal_02"
version = "0.1.0"
requires-python = ">=3.9"

dependencies = [
    # General dependencies
    "ipykernel",
    "python-dotenv",
    # RL dependencies (adapted for Apple Silicon ARM)
    "gym==0.26.2",
    "atari-py @ git+https://github.com/Kojoley/atari-py.git",
    "pyglet>=1.5",
    "h5py>=3.7",
    "Pillow>=9.5",
    "keras-rl2==1.0.5",
    "tensorflow>=2.13",
    "torch>=2.0",
    "agents==1.4.0",
    "numpy>=1.23,<2.0",
]

[tool.setuptools]
packages = []
